// Set to true to enable editing features (Upload, Save to Disk, etc.)
// When deploying, you might want to set this to false.
export const IS_EDIT_MODE = false;

// If true, the gallery will show random placeholder photos when galleryData.json is empty.
export const USE_RANDOM_DATA_IF_EMPTY = true;
